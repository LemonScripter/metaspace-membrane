"""Product B — Agent membrane (Claude Code PreToolUse hook).

Mediates the coding agent's own tool calls against a hardened session constitution
BEFORE they run. The membrane sits in the harness (outside the agent), which is what
makes it hard: not self-discipline, the hook blocks. Same core.guard engine as Product A.
"""
