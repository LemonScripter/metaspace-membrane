# Decisions & evidence

Every design decision is recorded so it can be reviewed independently. Every claim maps to
a runnable proof (see `evidence/demos/` and `products/*/`).

## Conceptual

| # | Decision | Rationale |
|---|---|---|
| C-1 | **Containment, not correctness.** The goal is not to prove code correct, but to bound its effects. | Rice's theorem: intent-conformance of arbitrary code is undecidable. |
| C-2 | **A bug = a deviation from the constitution.** | Makes "a bug" machine-measurable and enforceable. |
| C-3 | **Three membranes:** capability / value-invariant / epistemic. | Covers effect, value and fact hallucination. |
| C-4 | **Dual artifact from one `.bio`:** membrane (negative) + conformance test (positive). | The same source both locks and checks. |
| C-5 | **Provenance is communicated:** 🟢 RATIFIED vs 🟡 SYNTHESIZED. | Honest signalling of whether a human ratified the constitution. |
| C-6 | **Epistemic: hard vs soft split.** Referential integrity is hard; content faithfulness is soft (NLI). | Semantic faithfulness is not deterministic — it must not be claimed as hard. |
| C-7 | **Substrate target: WebAssembly + capability import; MVP language: Python.** | WebAssembly = unbypassable deny-by-default. |

## Implementation

| # | Decision | Rationale / limit |
|---|---|---|
| I-1 | **Name-based static effect detection** in the analyzer. | Simple, fast, enough for synthesis + CI. Aliasing/dynamism can evade it → the hard guarantee is the runtime membrane. |
| I-2 | **One shared decision engine** (`core.guard.check`) powers all layers. | One source, consistent decisions. |
| I-3 | **WebAssembly membrane uses named capability imports (a Linker).** | Name-based linking is the correct capability model and yields a precise `unknown import` error for ungranted gates. |
| I-4 | **Product A ships only the WebAssembly-hard variant.** | No over-promising: a bypassable language-level guard is not a product. |
| I-5 | **Agent membrane = a PreToolUse hook** in the harness, outside the agent. | An AI cannot be its own reference monitor — the membrane must sit outside it. |
| I-6 | **Fail-closed error handling:** unreadable input or unloadable constitution → deny. | A membrane that cannot see the request must not allow it. A loud block beats a silent bypass. |
| I-7 | **WASI capability model for real programs.** The filesystem preopen set is derived from the `.bio`; a WASI-compiled program (any language) can reach nothing else. | Standard, scalable containment of real compiled programs without per-app host functions. |
| I-8 | **Soft tier = pluggable entailment backends** (`core/entailment.py`): a dependency-free heuristic and a Claude LLM judge. Flags, never blocks. | Real semantic faithfulness is non-deterministic (C-6); the soft tier informs while the hard tier enforces. |
| I-9 | **Content-bound ratification** (`core/provenance.py`). The RATIFIED stamp carries a fingerprint of the enforced policy (capabilities + knowledge + bash). A post-ratification edit that changes the policy reads as TAMPERED. | Ratification (C-5) must not be forgeable by editing the file after approval. |
| I-10 | **Ratification gate** (`core/gate.py`, `Guard(require_ratified=True)`): in production only a RATIFIED constitution runs; SYNTHESIZED / TAMPERED is refused (fail-closed). | Closes the chain: a policy is enforceable only after human ratification and cannot be broadened afterwards. |
| I-11 | **Structural shell policy** (`core/shell_policy.py`): the agent membrane's shell check is now a lexer-based allowlist (real program names, obfuscation-resistant) + token-based denylist, replacing the substring heuristic. | Addresses external critique #2: a substring denylist both misses obfuscated commands and false-positives on harmless ones; a structural allowlist resolves the actual programs and fails closed. |
| I-12 | **Dry-run learning mode** (`core/dryrun.py`): before ratification, run the program once with effects recorded (observe, never block) and merge the concrete observed effects into the synthesized constitution. | Addresses external critique #3: static synthesis under-declares dynamic code, so a ratified static-only constitution false-positive-blocks legitimate behaviour and pushes developers to wildcards. The dry-run makes the ratified constitution match reality. |
| I-13 | **Threat-model matrix + strict terminology.** A reproducible matrix runs each layer's real check per attack vector. Terminology is fixed: *containment* = hard/structural block; *qualification* = the soft tier's non-blocking flag. | Addresses external critique #1: the soft/hard boundary must be explicit. The matrix shows honestly that a schema-valid but fabricated statement is NOT contained by the hard layers — only qualified by the soft tier. |
| I-14 | **Ratification review / cognitive brake** (`core/ratification_review.py`): dry-run-learned capabilities are *provisional* and must each carry a `JUSTIFY` reason; ratification is refused (fail-closed) otherwise, and `--yes` cannot bypass it. | Addresses the second review: the dry-run's convenience opens a risk of smuggling a capability past a fatigued approver. The brake forces a per-item, read-gated justification. Honest limit: it enforces a justification's *presence*, not its *truth*. |
| I-15 | **Complete capability mediation at the WebAssembly gate** (`products/app_membrane`): one guard-mediated host import per capability KIND — FILESYSTEM write/read, NETWORK, ENV, SUBPROCESS — each scope-enforced (in-scope ALLOW, out-of-scope DENY); an ungranted kind is unreachable by construction. | Delivers the "structural hardness" the reviews asked for: every declarable effect, not just filesystem, is hard-mediated at the unbypassable chokepoint. |
| I-16 | **Self-falsification audit** (`evidence/run_falsification.py`): the repo tries to prove itself hollow — mutating the `.bio` flips the decision (data-driven), sabotaging `guard.check()` makes a proof FAIL (code-coupled), and wasmtime itself refuses an ungranted syscall (real substrate). | Anti-slop guarantee: the proofs measure real behaviour, not print `PASS`. A rare self-test that the evidence is falsifiable. |
| I-17 | **Real program under the membrane** (`products/app_membrane/real_app`): a real Rust log-analysis utility (dir scan, per-file parse, aggregation, ranking, report) compiled to `wasm32-wasip1` and run under the WASI membrane; the report contains the CORRECT computed statistics for known input, and an out-of-grant read is refused. | Moves beyond toy demos: a real program doing real, verifiable work, contained to exactly its `.bio` grant. |
| I-18 | **Property-based fuzz** (`evidence/run_fuzz.py`): 5000 random constitutions x random effects check two properties — deny-by-default is sound (an ungranted kind/mode is never allowed) and the guard agrees with an independent scope oracle (traversal, absolute paths, host/name globs). | Moves from hand-picked scenarios to adversarial coverage; a fixed seed keeps it reproducible; a mismatch prints the counterexample. |
| I-19 | **Dogfood synthesis** (`evidence/demos/run_dogfood_demo.py`): the synthesizer runs over this repo's own ~36 Python files and produces a real, non-trivial constitution matching our code's actual effects (FS r/w, subprocess, env, real deps). | Refutes "the synthesizer only works on a toy sample"; the noisy invariants are shown honestly, warts and all. |
| I-20 | **M0 — `metaspace` CLI** (`cli.py`, pip entry point): one command over the engine — `synthesize / ratify / gate / report / init`, incl. a new human-readable session `report`. Cross-platform by construction (os.path/shlex, no OS-specific calls). | First step of productization (research prototype -> product). Honest gap: Linux/macOS CI is not run here (Actions billing-blocked); the `report` is the product's audit surface. |
| I-47 | **Agent-agnostic generalization — canonical design & roadmap (`docs/AGENT_AGNOSTIC_DESIGN.md`).** Recorded the plan to generalize the membrane from "the Claude Code Warden" to a universal effect-containment layer, grounded in the existing seam (`core.agent_adapter.decide` over a normalized `(kind, mode, target)` effect; the Claude hook and the MCP broker already prove the core is harness-independent). Two key framings: (1) **two generalization axes** — *agent breadth* (how many AI agents: hook / MCP-proxy / library adapters) vs *program breadth* (confine any process via the OS substrate: Landlock/seccomp + `.bio`) — do not conflate them; (2) **the OS substrate is the truly universal membrane** — it confines a Linux process regardless of whether it is an AI agent, an AI-written script, or `php-fpm`, which is exactly the DCC pillar (precedent: `dcc-shield` confines a non-AI process). Consequence: the substrate work delivers both the fallback for un-hookable agents AND WordPress/any-app protection — one engine, two payoffs. **Roadmap (user-approved sequence):** G1 `AgentAdapter` contract (refactor, zero behavior change) → G2 **MCP proxy** (interception in front of real MCP servers; biggest ecosystem reach) → G3 **universal substrate** (generalize `metaspace run` from one-shot Python to any process/language; partly exists via `run`/`verify`) → G4 **WordPress PoC** on the G3 substrate (`php-fpm` confined; "compromised plugin cannot write core / cannot exec"), kept as a separate PoC so substrate targets never fork focus. Each phase ships a new falsifiable proof. Honest guarantee ladder per pattern (interception/broker = hard given the routing/ambient-authority condition; library decorator = cooperative guardrail; substrate = hard but coarser, Linux hard tier). Positioning stays honest: the membrane does not *detect* hallucination, it makes its *effect* unreachable (containment, not correctness). Monetization: MCP proxy + hosted substrate = the BSL Competing-Use / Pro surface. No code yet — this locks the canon before G1. | Generalizing "any agent" is not one adapter but a small set of enforcement patterns keyed to where effects cross a controllable boundary; writing this down before coding prevents scope drift and keeps the guarantee claims honest per pattern. WordPress validated the universal-substrate vision and is squarely within the DCC pillar, but sequenced as a separate PoC after the just-launched AI-agent beachhead (anti-regress). |
| I-46 | **License flip: Proprietary → Business Source License 1.1 (open-core), in effect now.** The old proprietary "permission is NOT granted to use" LICENSE was self-contradictory for a `pip install`-able developer tool and a direct adoption blocker — it repels the OSS-developer target audience and drags the plugin-hub Trust signal. Replaced `LICENSE` with the canonical BSL 1.1 text carrying the previously drafted parameters: **Additional Use Grant** = free for any non-competing use (internal, research, education, and products that merely depend on it); **Competing Use** = offering a commercial/hosted safety-guardrail-membrane product for AI software; **Change Date** = the 4th anniversary of each version's release; **Change License** = Apache-2.0. This grants use *now* while keeping the patent moat (unlike Apache's immediate patent grant). Synced across `pyproject.toml` (`license = "BUSL-1.1"` + PyPI classifiers + repository/issues URLs), `.claude-plugin/plugin.json` (`"license": "BUSL-1.1"`), `README.md`, and `docs/LICENSE.BSL.draft.md` (reframed from "not in effect" to "in effect; counsel refinement is a follow-up"). Honest caveats recorded for counsel: diff the canonical BSL body against mariadb.com/bsl11 verbatim, and finalize the Competing-Use scope / per-version Change Date / patent interaction — a follow-up, not a blocker to the granted rights. Prerequisite for the PyPI + adoption push (user decision, 2026-07-18). | A source-available license that actually permits use is the real gate for adoption via PyPI and the plugin marketplace; BSL unblocks the OSS-developer audience today without surrendering the patent moat. |
| I-45 | **F4-6 — a plain-language "How do these tools work?" help modal on the Tools card, in EN/HU/RO.** Answers the exact confusion the user hit: why the tools take a specific entry file (e.g. `app.py`), whether they only work on Python, and whether every `.py` must be checked. Root cause of the confusion: `run`/`verify` *execute* the target (`compile`+`exec` as `__main__`), so they need the one file you'd start with `python` — a single entry point that covers the whole run (imported modules included, because the effect interception is process-global), not a per-file scan. The Tools card gained an "ⓘ How do these work?" trigger opening a `<dialog class="modal2">` (reusing the existing `cmdinfo` pattern) with an in-modal EN·HU·RO switch (remembered in `localStorage`, default EN, matching the trilingual landing). The guide explains both tools plainly — 🔍 authenticity check (`verify`: real or "slop"? CONSISTENT/HOLLOW/HIDDEN-EFFECT/NO-EFFECTS) and 🛡️ run under the app membrane (`run`: deny-by-default, allowed vs blocked) — and states the honest limits (claims-vs-effects, not general correctness; Python only; reads never affect the verdict; GUI scripts need a headless run; the hard boundary for untrusted binaries is Landlock on Linux). Copy is a self-contained inline JS object — zero deps, no panel-wide i18n framework. Demonstrated live before writing the copy: `metaspace verify` on external code (`_metaspace_kolumban`) — `illusztracio.py` (has `savefig`) → CONSISTENT, `metaspace_lyapunov_demo.py` (animation only) `--expect writes` → HOLLOW (both `MPLBACKEND=Agg`). Proven by the extended `run_ui_proof.py` (the page ships `id="toolsinfo"` + one sentinel of each EN/HU/RO copy). Verified Win (37 pass + 1 skip of 38, clean env); Linux (Tokyo) re-run pending. | The panel is the non-CLI surface, and the entry-file question is the first thing a real user trips on — answering it in plain language, in the user's own three languages, turns the Tools card from confusing into self-explaining. |
| I-44 | **F4-5 — the panel surfaces `run` / `verify` / `license`.** Fixes a real UX gap the user hit ("I don't see any change in the UI"): the F6/F7/F8 features were CLI-only, so the panel looked unchanged. The control panel now exposes three new endpoints — `/api/run` (app membrane: reports allowed vs blocked effects for a Python file, using the folder's own `.bio`), `/api/verify` (authenticity gate: verdict for a file), `/api/license` (GET status, POST activate/remove an offline key) — plus matching UI cards (a "Tools" card and a "Licence" card showing FREE/PRO). Each executes user-chosen local code, which is safe by the same design that makes the features safe: `verify` runs under the recording membrane, `run` under the deny-by-default app membrane; and all inherit the panel's existing self-defence (127.0.0.1 + per-launch token + same-origin), so no web page can trigger them. Kept the enforcement engine shared (the panel calls the same `core` modules, not a reimplementation). Honest note carried into the UI copy: the Licence card states every feature is free right now — a key only records the tier — matching the "infra now, nothing gated" decision (I-41). Proven by the extended `run_ui_proof.py` (now also: panel verify → NO-EFFECTS/HIDDEN-EFFECT, panel run → granted-allowed/undeclared-blocked, panel licence → activate valid / reject invalid / remove). Verified Win + real Linux (Tokyo) 38/38. | The panel is the surface non-CLI users actually see, so putting the app membrane, the slop gate and licensing there is what makes the product feel whole — and doing it by calling the same proven core keeps one enforcement engine, not two. |
| I-43 | **F6-2 — privacy-first analytics (`analytics/server.py`) + opt-in CLI upload.** Delivers the user's "there should be statistics" without compromising the trust a security tool depends on. Design: the service *counts and nothing else* — cookie-free, no IP/user-agent stored, no per-event rows; the entire store is one `counts(key,n)` SQLite table, so it is structurally incapable of answering "who", only "how many". Endpoints `POST /a` (landing visit/CTA), `POST /t` (opt-in CLI action), `GET /stats` (JSON + a tiny dashboard); permissive CORS so the landing/CLI can post. Robustness by construction: every stored token is sanitised to `[a-z0-9_.-]`, lower-cased and length-capped, and a malformed body returns 400 while the server keeps serving — a hostile beacon can neither inject (`DROP TABLE …` becomes an inert counter key) nor crash it. The CLI side (`core/telemetry.py`) uploads **only the coarse event name**, and only when consent is on AND `METASPACE_ANALYTICS_URL` is set (no default URL, no id sent, fire-and-forget in a daemon thread, never fatal) — so the earlier "off by default / no PII / not on the hot path" guarantees are preserved. Stdlib only (keeps the zero-dep ethos); Railway deploy via `analytics/Dockerfile` with the DB on a mounted volume so counts survive redeploys. Proven no-mock by `run_analytics_proof.py` (P-ANALYTICS): real server on 127.0.0.1, real HTTP; counts accumulate, CORS + preflight work, malformed→400+survives, the schema is only `counts(key,n)`, and hostile SQL/script/500-char input is neutralised. Verified Win + real Linux (Tokyo) 38/38. PyPI publish (download counts) is the remaining tracking piece and needs the maintainer's token. | Lets us see adoption (page views, CTA, opt-in usage) while being able to say honestly that the analytics *cannot* deanonymise anyone — the store has no field for it — which is the only kind of telemetry a deny-by-default safety product can credibly ship. |
| I-42 | **F8 — the app membrane is now a usable command (`metaspace run`).** Answers the user's "we must be able to use the app membrane too, not only the agent Warden": a *running program* (including AI-written code) is confined to its `.bio` deny-by-default. Two backends, chosen by target/OS: (1) **Python target, any OS** — `core/apprun.py` runs the program in-process, intercepting `open`(write)/`socket`/`create_connection`/`Popen`/`os.system` and routing each through `core.guard.Guard.check` (the same deny-by-default engine as the agent hook), so granted effects hit the real resource and undeclared ones raise and are blocked; reads pass through (MVP scope — confine *effects*, not reads, mirroring Landlock); it substitutes `{{PROJECT_ROOT}}`/`{{CLAUDE_HOME}}` like the hook and `chdir`s into the root so relative paths resolve as the guard normalises them; (2) **native binary, Linux** — delegates to the existing M3 `sandbox_enforcer.py` (Landlock, kernel-enforced write confinement), a hard boundary; fail-closed on non-Linux for native targets. Reuses the Guard rather than a second policy engine, so the app membrane and the agent membrane share one deny-by-default core. Proven no-mock by `run_apprun_proof.py` (P-APPRUN): a granted write (absolute AND relative) really lands on disk; an out-of-scope write, a network call and a subprocess are blocked; and on Linux a native program's out-of-scope write fails at the kernel (EACCES). Bugs the live demo caught and that the proof now guards: missing `{{PROJECT_ROOT}}` substitution and missing `chdir`. **Honest scope:** the Python backend is a *language-level* membrane — usable and cross-OS and a real deny-by-default gate for ordinary/AI-written programs, but bypassable by adversarial native code that rebinds builtins; the hard boundary for untrusted binaries is the Landlock backend (Linux). Verified Win (36 pass + 1 skip) + real Linux (Tokyo) 37/37. | Makes the "contain effects, not the model" thesis concrete on the *application* side, using the same core as the agent guard so there is one enforcement engine to trust — while stating plainly which backend is a hard boundary and which is a usable-but-bypassable convenience. |
| I-41 | **F6 — open-core licence infrastructure (`metaspace license`/`keygen`/`issue`), built now but non-activated ("infra now, everything free").** Monetisation follows the project's honest-draft pattern (like the BSL draft): the mechanism ships and is proven, but no shipped command refuses to run without Pro yet — the user chose adoption-first. `core/license.py` verifies Ed25519-signed licence tokens **fully offline** against an embedded vendor public key (env-overridable) — no phone-home, works air-gapped. Design invariants: (a) a licence is a **soft entitlement gate, not a security boundary** (containment stays `core/guard.py`); (b) the **free Warden membrane keeps zero third-party deps** — the crypto lives in the optional `[pro]` extra, loaded only on the licence path, never on the enforcement hot path; (c) the placeholder embedded key's private half was **discarded on purpose** so no valid key can be issued until the vendor runs `keygen` and sets their own. `is_pro()` exists so a single gate flips a feature Pro later. Payment provider deliberately deferred — the offline design is provider-agnostic (a webhook just calls `issue`). Proven no-mock by `run_license_proof.py` (P-LICENSE): a genuine key verifies (tier=pro), and tampered / cross-key-forged / expired keys are all rejected; install/read-back/remove round-trips; free is the safe default. Verified Win + real Linux (Tokyo) 36/36. | Lets us stand up the business model without a hosted licence server or an online-activation dependency, and without compromising the free membrane's zero-dep, offline, self-contained promise — while being honest that nothing is charged for yet and that licensing deters casual copying, not a determined patcher. |
| I-40 | **F7 — the authenticity gate (`metaspace verify`).** Realizes the deep-analysis answer to "can the membrane detect AI slop?": the membrane is the one place a program's REAL effects are observable, so it catches the commonest, most damaging slop — code that *looks* like it works but produces no effect. `core/verify.py` runs a Python target under a recording membrane that is safe by construction (filesystem writes redirected into a throwaway sandbox; network and subprocess recorded then blocked; reads normal — with no network there is nothing to exfiltrate to), captures the effect-trace, and `analyze()` compares observed effect categories to the claimed ones: claimed-but-never-attempted → **HOLLOW**, undeclared network/subprocess → **HIDDEN-EFFECT**, match → **CONSISTENT**, nothing claimed and nothing done → **NO-EFFECTS** (so pure computation is not falsely flagged). `metaspace verify <app.py> --expect writes|network|subprocess` exits non-zero on hollow/hidden (CI-usable). Proven no-mock by `evidence/run_slopgate_proof.py` (P-SLOPGATE): three real programs — genuine (writes) → CONSISTENT, slop (prints "saved 1,000,000 rows!" but writes nothing) → HOLLOW, and one that phones home to an undeclared host → HIDDEN-EFFECT — are distinguished by their effects, not their identical-looking success output. **Honest scope (as designed, stated in the code):** detects the claims-vs-effects gap for Python; it is NOT a general correctness/quality checker (Rice's theorem) — a program with the right *kind* of effect can still be logically wrong. Verified Win + real Linux (35 proofs; 35/35 Linux, 34 pass + 1 skip Windows). | The flagship differentiator, and the honest one: the same effect-observation that contains a malicious agent also exposes a hollow one, so "does this AI-generated code actually do what it says?" becomes a runnable check rather than a matter of trust — while we refuse to over-claim it as a universal slop oracle. |
| I-39 | **F4-4 — command picker with type-to-search + an info list.** Choosing which programs the agent may run is the panel's most consequential control, so it should not be a blank text box. `core/command_catalog.py` holds a curated catalogue of common developer commands, each with a one/two-sentence explanation of what allowing it lets the agent do (and its risk — e.g. `curl` "a common exfiltration tool", `rm` "keep an 'rm -rf' pattern on the always-blocked list"). The panel's "Allowed commands" field uses a native `<datalist>` for type-to-search autocomplete over the catalogue (custom entries still allowed), and a "What can these do?" modal lists every command alphabetically with a live filter. Served at `/api/commands`; stdlib-only. Proven by `run_commands_proof.py` (P-COMMANDS: alphabetical, every entry has a real explanation, every shipped-default command is covered, high-risk commands present) and the extended `run_ui_proof.py` (the endpoint serves the catalogue). Also hardened the UI proof against a background-server timing flake (retry on any transient connection error + a readiness gate). Verified Win + real Linux (34 proofs; 34/34 Linux, 33 pass + 1 skip Windows). | Turns a scary blank field into a guided, self-documenting choice for the vibecoder — the person deciding what an AI agent may run finally gets told, in plain language, what each option actually permits. | 
| I-38 | **F4-3 — panel polish + opt-in, privacy-first telemetry.** The panel gained: editing an existing folder's rules (fetch its fields, change, save — the hook reflects it), a per-folder activity view (allowed / blocked / would-block, from the project's audit), and a usage-stats toggle. Telemetry (`core/telemetry.py`) is deliberately minimal because a security tool runs on trust: **off by default** (record is a pure no-op until opt-in), **anonymous** (a random id created only on opt-in, forgotten on opt-out), **no PII ever** (a fixed event vocabulary + int/bool counters only — any string a caller passes is dropped, so no path/filename/command/code is stored), **local-only** in this scaffold (network delivery stays a later, opt-in phase), and **never on the enforcement hot path** — only coarse CLI actions (`install/enforce/dryrun/off/ui_open`) record, so the hook adds zero telemetry cost or surface. `metaspace telemetry on|off|status` controls it from the CLI too. Proven no-mock by `evidence/run_telemetry_proof.py` (P-TELEMETRY: default-off no-op, anonymous id, PII dropped, unknown events rejected, opt-out forgets) and the extended `run_ui_proof.py`. Verified Win + real Linux (33 proofs; 33/33 Linux, 32 pass + 1 skip Windows). | Builds the "track downloads/usage properly" requirement in a way that a privacy-conscious, security-minded audience will accept — consented, anonymous, inspectable, and structurally incapable of leaking code or PII — rather than as invasive telemetry that would poison the product's core value (trust). | 
| I-37 | **F4-2 — `metaspace ui`, a self-defending localhost control panel.** A browser panel (`products/ai_membrane/ui_server.py`, stdlib `http.server`, zero deps) lets a user configure the membrane per working directory in plain language — pick a folder, set what the agent may write/reach/run, choose Observe/Enforce — which `core/bio_fields.py` translates to/from a real `.bio`, **always injecting the self-protection deny** so no UI input can produce a disable-able config. Because the UI edits security-critical files, it defends itself: binds `127.0.0.1` only, a random per-launch token gates every request, and state-changing requests must be same-origin localhost (Host + Origin checks) — so even if a malicious website guessed the port it cannot drive the panel. Proven no-mock by `evidence/run_ui_proof.py` (P-UI-API + P-UI-CSRF): the real server on a background thread is driven over real HTTP, a configured project then actually changes the REAL hook's verdicts, and no-token / cross-origin requests get 403. `metaspace ui` launches it; `metaspace projects` lists configured dirs. Verified Win + real Linux (32 proofs; 32/32 Linux, 31 pass + 1 skip Windows). | Delivers the "install then configure from a UI" requirement for amateur-friendly use, without weakening the security model — the config surface is itself deny-by-default against the web, and the friendly fields cannot express an unsafe (self-disabling) constitution. | 
| I-36 | **F4-1 — per-working-directory constitutions, stored user-level.** To let a user configure the membrane differently for each working directory (the control-panel requirement) WITHOUT breaking self-protection, every per-project constitution lives under `~/.claude/metaspace` — a registry (`registry.json`, mapping absolute project path → {hash, mode, label}) plus `projects/<hash>.bio`. The hook (`core/project_config.resolve`) looks up the current project (from `CLAUDE_PROJECT_DIR`) and uses its constitution + mode, falling back to the install default when a project is unregistered. Because the configs sit under `~/.claude` (outside any project's write scope, and covered by the `FILESYSTEM deny "{{CLAUDE_HOME}}/**"` rule), a deceived agent still cannot edit them — self-protection is preserved per project, not just globally. `core/project_config.py` is the single source of truth for both the hook and the coming UI/CLI; stdlib-only (zero deps). Proven no-mock by `evidence/run_project_config_proof.py` (P-PROJECT-RESOLVE): two projects with different allowlists AND different modes each resolve correctly, an unregistered project falls back to the default, and an agent (even at HOME) cannot write the registry. Verified Win + real Linux (31 proofs; 31/31 Linux, 30 pass + 1 skip Windows). | Delivers per-project configurability — the foundation of the control panel — while keeping the security property that made user-level install the right call: the membrane's config, now per-project, is still unreachable by the attack it defends against. | 
| I-35 | **Correction + GitHub Actions disabled repo-wide.** Earlier entries (I-20, I-25, I-34) and some docs said hosted Actions was "billing-blocked" here. That was inaccurate: after the v0.2.0 work was pushed, the `proofs` and `metaspace-gate` workflows actually *ran* on every push/PR and *failed* within ~3s (an account-level Actions restriction, not code failure), producing a stream of failure-email notifications. To stop the noise we **disabled GitHub Actions for the repository** (`actions/permissions → enabled:false`, free). The two workflow `.yml` files remain in the tree but are inert (removing them needs a token with the `workflow` scope, which the current one lacks); they are harmless with Actions off and adoptable on forks (a fork's own Actions are independent). **Net honest position:** there is no hosted-CI badge; the whole suite is verified every change locally on Windows + real Linux (Tokyo GCP), which is the actual evidence. Prior "billing-blocked" wording corrected in README / INSTALL_PLAN / run_team_gate_e2e; the append-only DECISIONS rows are left intact and superseded by this note. | Keeps `claim = proof` honest: a mistaken infrastructure claim is corrected rather than quietly edited away, and the fix (disable Actions) both stops the email spam and makes the "no hosted CI" statement literally true. |
| I-34 | **WI-8 — release 0.2.0; F1 complete.** Version bumped 0.1.0 → **0.2.0** across `pyproject.toml`, `.claude-plugin/plugin.json`, and `cli.py` (P-VERSION green); `CHANGELOG.md` `[0.2.0]` section finalized (2026-07-13); added a `proofs` GitHub Actions workflow (ubuntu + windows matrix running `python run_proofs.py`, adoptable on forks). This closes F1 (plugin-turnkey easy install): the full journey — `metaspace install` (user-level, dry-run) → `demo` → `report` → `enforce` → `off` — is shipped, self-protecting, and covered by **30 proofs** (30/30 real Linux, 29 pass + 1 skip Windows; the Warden hook is zero-dependency). **Honest limits carried forward:** macOS is unverified (no host); hosted Actions is billing-blocked here (no green-badge claim; the workflow is for forks); a standalone binary for the no-Python segment is a later phase. The `v0.2.0` tag and the PR of `feat/warden-install` into `main` are the user's final push. | Ships the install as a proven, versioned product surface. The whole F1 kept `claim = proof`: every user-facing capability landed with its own falsifiable, no-mock proof wired into the one command that gates the branch. |
| I-33 | **WI-6 — version parity + `metaspace --version`.** The version now lives in three places a user or tool can read — `pyproject.toml` (pip), `.claude-plugin/plugin.json` (the Claude Code plugin), and `cli.py` `__version__` (surfaced by `metaspace --version`). `evidence/run_version_proof.py` (P-VERSION) fails the suite if they disagree, so a release that bumps one and forgets another cannot pass. The plugin manifest set (`plugin.json` + `marketplace.json` + `hooks/hooks.json`, the last wiring the PreToolUse hook via `${CLAUDE_PLUGIN_ROOT}`) is confirmed consistent. **Honest note:** the plugin path enforces immediately (no env is injected, so `METASPACE_MODE` is unset → default enforce), whereas the recommended `metaspace install` starts in dry-run; installing the plugin is a deliberate opt-in, so immediate enforcement is acceptable and documented. Verified Win + real Linux (30 proofs; 30/30 Linux, 29 pass + 1 skip Windows). | Makes the version a single fact checked by a proof rather than three hand-synced strings — the release step (WI-8) can bump with confidence, and a drift is caught by `run_proofs.py`. |
| I-32 | **WI-9 — `metaspace off`, a human-only uninstall.** One command removes the PreToolUse hook and the `METASPACE_*` env keys from `settings.json` while preserving every other setting and hook; it is idempotent (a second `off` is a clean no-op) and `--purge` additionally deletes the installed constitution. Crucially it is a *human* action the agent cannot trigger — `metaspace` is not on the shell allowlist, so a deceived agent's `metaspace off` is blocked at the tool-call gate, and `~/.claude/settings.json` is outside its write scope. Proven no-mock by `evidence/run_uninstall_proof.py` (P-UNINSTALL): real install then real `off` in a temp HOME, asserting the hook is gone, unrelated settings/hooks/env survive, a second off is a no-op, and the agent's `metaspace off` via Bash is BLOCKED. Verified Win + real Linux (29 proofs; 29/29 Linux, 28 pass + 1 skip Windows). | Completes the locked "explicit off" decision (I-27d) without opening a disable route to the attacker: the off switch exists for the user and is closed to the agent — the same asymmetry as the rest of the design. |
| I-31 | **WI-5 — `metaspace demo`, a live self-test that is also a proof.** After install a user can run `metaspace demo`: it materializes the Friendly-Fire attacker artifacts in a throwaway repo and drives the REAL PreToolUse hook (in enforce mode) over every attack vector, printing each malicious effect BLOCKED and normal work allowed, and exiting non-zero if anything leaks. Because it spawns the actual hook rather than printing a verdict, a green demo is a real end-to-end block — the "wow" onboarding moment doubles as evidence. `evidence/run_demo_proof.py` (P-DEMO) runs `metaspace demo` and asserts exit 0, that attack effects are BLOCKED, that no ALLOWED-attack line appears, and that it concludes "the RCE never happens". **Honest note:** the demo forces enforce mode for the self-test regardless of the installed `METASPACE_MODE`, so a user in dry-run still sees what blocking looks like. Verified Win + real Linux (28 proofs; 28/28 Linux, 27 pass + 1 skip Windows). | Converts a trust problem into a five-second demonstration on the user's own machine, and keeps it honest: the demo cannot pass unless the shipped membrane actually blocks the attack. | 
| I-30 | **WI-4 — dry-run/observe as the default first run.** A security tool that over-blocks on minute one gets uninstalled, so a fresh `metaspace install` starts in dry-run: the hook reads `METASPACE_MODE` and, when a decision is BLOCK, in dry-run it records the decision as a would-block, warns loudly on stderr, and *allows* the effect through; `metaspace report` surfaces what would be blocked; `metaspace enforce` flips `METASPACE_MODE` to enforce (and `metaspace dryrun` flips back). `metaspace install --enforce` skips dry-run for users who want blocking immediately. Mode-flip commands edit `settings.json` — a human action the agent cannot reach (self-protection: `~/.claude` is outside the write scope and `metaspace` is not shell-allowlisted). Proven by `evidence/run_dryrun_mode_proof.py` (P-DRYRUN, no mock): a real install defaults to dry-run and an attack vector is allowed-but-recorded-as-would-block; after `metaspace enforce` the same vector is blocked. **Honest note:** dry-run means there is NO enforcement during the observation window — the user must review and `enforce`; this is the deliberate onboarding trade (default enforce if `METASPACE_MODE` is unset). To keep the install proof honest, P-INSTALL now installs with `--enforce` so its block assertions reflect a live membrane, while P-DRYRUN covers the default observe→enforce flow. Verified Win + real Linux (27 proofs; 27/27 Linux, 26 pass + 1 skip Windows). | Resolves the adoption tension the reviewers on prog.hu raised (an over-strict gate cripples the agent): the membrane earns trust by first showing what it would do, then blocking on the user's word — without ever letting the agent turn on/off that word. |
| I-29 | **WI-3 — self-protection: the membrane cannot be disabled by the attack it defends against.** A new **FILESYSTEM write deny-override** in `core/guard.py` blocks a write to any path listed under `FILESYSTEM deny` *even when an allow-scope would match it*; the shipped constitution uses it as `FILESYSTEM deny "{{CLAUDE_HOME}}/**"`, where the hook substitutes `{{CLAUDE_HOME}}` with `~/.claude`. This closes the worst case: if Claude Code is opened at the home directory, `{{PROJECT_ROOT}}/**` (= `~/**`) would otherwise grant writes to `~/.claude/settings.json` and the constitution itself — the deny-override forbids it. The other disable route, running `metaspace off`/`metaspace ratify` as a shell command, is already blocked because `metaspace` is not on the shell allowlist. So only the human, outside the agent's tool calls, can disable the membrane. Proven no-mock by `evidence/run_selfprotect_proof.py` (P-SELFPROTECT): a real install into a temp HOME, then the real hook with `PROJECT_ROOT=HOME` blocks writing/editing settings and the constitution and running `metaspace off`/`ratify`, while a normal in-project write (outside `~/.claude`) is still allowed. Verified Win + real Linux (26 proofs; 26/26 Linux, 25 pass + 1 skip Windows). | Turns the locked design property (I-27b) into an enforced, falsifiable guarantee: the deceived agent that the whole product assumes cannot switch the product off. The deny-override is a general capability (deny beats allow for filesystem writes), reusable for any path a constitution must protect. |
| I-28 | **WI-2 — `metaspace install`, user-level by default.** A one-step installer merges the PreToolUse hook into `~/.claude/settings.json` and drops an editable constitution into `~/.claude/metaspace/`. Design points: (a) **user-level default** — the installer does NOT pin `METASPACE_PROJECT_ROOT`; the hook resolves the project per session from `CLAUDE_PROJECT_DIR`, so one install covers every project AND `~/.claude` stays outside any project's write-scope (the structural basis of self-protection, proved in WI-3); (b) `--project DIR` is offered for teams but prints a caveat that a project-local config is agent-reachable; (c) the hook entry uses the same **single command-string** form as the plugin `hooks.json` (`{"type":"command","command":"python \"…\""}`), not a separate `args` array; (d) **idempotent + non-clobbering** — a prior MetaSpace hook is replaced (matched by the hook filename), every other setting/hook is preserved, a second install does not duplicate. Proven no-mock by `evidence/run_install_proof.py` (P-INSTALL + P-IDEMPOTENT): it runs the real installer into a temp HOME, inspects the real `settings.json`, then drives the REAL hook with the INSTALLED constitution and confirms it blocks the Friendly-Fire vectors and allows legit work. **Honest limit:** the shipped default currently installs in *enforce* mode; dry-run/observe as the default first-run state is WI-4, and clean-revert uninstall (`metaspace off`) is WI-9 — both still pending. Verified Win + real Linux (25 proofs; 25/25 Linux, 24 pass + 1 skip Windows). | Turns "install the protection" into one command whose correctness is itself a proof, and bakes the security property into the install *location*: a user-level membrane cannot be switched off by the very attack it defends against (fully proven once WI-3 lands). |
| I-27 | **Easy-install architecture (Warden, F1) — user-level, self-protecting, dry-run, explicit off.** Planning decision for a one-step install (branch `feat/warden-install`, target 0.2.0; blueprint in `docs/INSTALL_PLAN.md`). Four choices: (a) **user-level only** (`~/.claude/`) so the membrane's own config sits *outside* the project write-scope — the same deny-by-default rule that stops the attack also stops the attack from disabling the membrane; (b) **self-protection is part of the threat model**: a deceived agent must fail on every disable route — writing settings/constitution via the hook (outside scope) AND running `metaspace off`/`ratify` as Bash (not on the shell allowlist) — only the human, outside the agent's tool calls, can disable it; (c) **dry-run/observe is the default first-run** state (a security tool that over-blocks on minute one gets uninstalled); (d) an **explicit `metaspace off`** exists as a human action. **Anti-slop systematized** alongside: a claim ledger (`docs/CLAIMS.md`, claim = proof), a per-work-item Definition of Done (every feature ships a *falsifiable*, no-mock proof wired into `run_proofs.py`), falsification-first, and CI running the whole suite. **Honest limit:** this is a plan; each claim flips to [PROVEN] only when its proof (P-ZERODEP, P-INSTALL, P-SELFPROTECT, P-IDEMPOTENT, P-DRYRUN, P-DEMO, P-UNINSTALL, P-VERSION) is green; macOS stays unverified until a host exists; distribution as a standalone binary (no-Python segment) is a later phase, not F1. | Makes the install a first-class, proven part of the product rather than a README afterthought — and encodes the key insight that *where* the membrane installs is a security property, not a packaging detail: a project-level install could be switched off by the very attack it defends against, so user-level + agent-unreachable disable is the design, proven by P-SELFPROTECT. |
| I-26 | **Interpreter-passthrough hardening + Friendly Fire proof.** The AI Now Institute "Friendly Fire" brief (2026) shows a prompt-injected coding agent steered by a malicious repo README into running a repo-local script that launches a malicious binary (RCE), with no user approval. Two changes: (a) `core/shell_policy.py` now treats a shell interpreter (`sh/bash/zsh/dash/ksh/ash`) as a would-be allowlist bypass — allowlisting one lets it run ANY program — so an interpreter invocation is only allowed when what it will run is itself allowlisted: `bash x.sh` requires `x.sh` allowlisted, `bash -c "CMD"` re-checks CMD recursively, `... | bash` (unverifiable piped stdin) is denied, bare `bash` is harmless. (b) `evidence/run_friendly_fire_proof.py` drives the REAL Warden hook over the full attack: it builds a victim repo with the actual artifacts (injected README, `security.sh`, decoy `code_policies.go`, marker payload), ASSUMES the model is fully deceived, and asserts every code-execution path is BLOCKED (7/7: direct script, direct binary, and every shell-wrapper indirection), plus exfiltration and out-of-project persistence, while legitimate work (`python build.py`, `git status; ls`, `sh -c "python build.py"`, in-project write, allowed-host fetch) still passes. **Honest scope of the claim:** the RCE *effect* is unreachable at the tool-call boundary (deny-by-default) for this attack class; NOT covered here (covered elsewhere) — an *allowlisted* interpreter running its own trusted script is allowed by design, and confining a process that DID start is the hard substrate's job (Landlock/WASM, I-24/I-15). | Answers the brief's core thesis — "inherent LLM limitation, not patchable, human approval fails to prevent it" — structurally: we never try to DETECT the injection (a losing whack-a-mole; the brief notes 3 config-vector patches in 6 months), we make the EFFECT unreachable regardless of how the model was convinced, with a deterministic O(1) verdict and no human prompt (so no automation-bias/fatigue). Closes a real gap the proof itself found (`bash security.sh` slipped the earlier program-name-only allowlist). |
| I-25 | **M4 (part) — team / CI gate on an in-repo ratified `.bio`.** A repository commits a ratified `metaspace.bio`; `metaspace gate` (M0) makes "the effect boundary here is human-ratified and unbroadened" a build-breaking condition, using the same content-bound fingerprint as the runtime gate. Shipped as adoptable artifacts: a GitHub Actions workflow (`.github/workflows/metaspace-gate.yml`) and a pre-commit hook source (`.pre-commit-hooks.yaml`). **Honest limit:** the hosted Actions run cannot be self-verified on this repo (Actions billing disabled), so no green-badge claim; the gate *logic* is proven locally and cross-OS by `run_team_gate_e2e` (unratified → build red; ratified → green; broadened-after-ratify → TAMPERED → red). The other half of M4 — the **enterprise BSL layer** — is a licensing change that needs IP counsel; a clearly-marked, non-activated draft is in `docs/LICENSE.BSL.draft.md`, and `LICENSE` remains Proprietary until sign-off. | Delivers the team surface without depending on hosted CI: the gate is a local command whose exit code is the build signal. Keeps the claim honest (no self-verified Actions badge) and keeps legal out of scope for the engineering. |
| I-24 | **M3 — SandboxEnforcer: a real native program OS-confined by its `.bio`** (`products/app_membrane/sandbox_enforcer.py`, pure-Python `ctypes`, no dependency). Given a constitution, it derives the FILESYSTEM write scopes and applies a **Linux Landlock** ruleset to the process before `exec`-ing the target, so the guarantee is the *kernel's* and holds for ANY binary (not only WASM-compiled ones) — the OS analogue of the WebAssembly substrate. **Honest MVP scope:** the ruleset governs only filesystem-*modifying* operations (write/create/delete/rename); read + execute are left unrestricted so any dynamically-linked program still runs. So the claim is precise: *a program under the enforcer can only change the filesystem where its `.bio` allows; an out-of-scope write is refused with EACCES by the kernel.* Read/execute confinement is a Landlock capability, deferred. **Fail-closed:** if Landlock is unavailable, the enforcer refuses to launch (never runs a program unconfined while claiming to confine it). Building it also surfaced and fixed a **latent bug in the shared parser** (`parse_capabilities` broke its scan on *any* line containing `}` — including the `{{PROJECT_ROOT}}` placeholder; every existing caller masked it by substituting the placeholder *before* parsing, so it never bit; hardened to end the block only on a line that *starts* with `}`). | Delivers Product A beyond WASM: a real, unmodified native program contained by the kernel from a `.bio`. It is an OS-specific hard guarantee, so its proof RUNS on Linux and SKIPs honestly elsewhere (the runner is skip-aware). Verified on real Linux (Debian, kernel 6.1, Landlock ABI v2). |
| I-23 | **M2 — one decision core, many harnesses (MCP capability-broker).** The membrane's decision is factored into a harness-independent core (`core/agent_adapter.py`: `decide(kind, mode, target, guard, …)` over `core.guard` + `core.shell_policy`). The Claude Code hook is now a thin adapter that maps tool events onto `(kind, mode, target)` and calls it; a second adapter, a **generic MCP capability-broker** (`products/mcp_membrane/server.py`, a minimal JSON-RPC 2.0 stdio server: `initialize` / `tools/list` / `tools/call`), exposes *mediated effect tools* (`fs_write`, `fs_read`, `net_fetch`) gated by the same `decide`, deny-by-default, refusing (and not performing) out-of-constitution effects. **Honest guarantee condition:** an MCP `check`-style tool the agent may voluntarily call is only advisory (self-discipline, which we reject); the broker is *hard* only when the agent is deployed with its ambient authority removed so the broker's tools are its ONLY path to effects — given that, it equals the hook's harness-hook tier. **Honest scope:** FILESYSTEM effects are gated AND performed (real, hermetic); NETWORK is gated (verdict), with egress deferred so proofs never beacon. | Makes "the same core on a different harness" literally true, not a slogan: the adapter-proof drives BOTH harnesses over their real interfaces and shows identical verdicts. This is the M2 platform step — the guarantee lives in one place; adding a harness (Cursor, generic MCP client) is a thin, provable adapter, not a re-implementation. |
| I-22 | **M1 — the Warden product loop, proven end-to-end** (`evidence/run_product_e2e.py`): on a realistic project, the *actual* PreToolUse membrane hook is driven exactly as the Claude Code harness invokes it (subprocess, PreToolUse JSON on stdin, `CLAUDE_PROJECT_DIR` env) across a realistic agent session (write in-project ALLOW, write-outside BLOCK, read ALLOW, pytest/git-status ALLOW, git-push/rm-rf/curl-pipe BLOCK, allowed-host WebFetch ALLOW, beacon BLOCK). The hook enforces the *shipped default* constitution, writes a *project-local* audit (`.metaspace/session_audit.jsonl`), and the *real* `metaspace report` summarizes it. Nothing is mocked (unlike `run_cli_e2e`, which drives the CLI over a hand-written audit). Building it surfaced and fixed two real robustness defects in `core/guard.py`: (a) the guard's audit log now **auto-creates its parent dir and is fail-soft** — a logging I/O error can no longer crash a decision, and cannot suppress a DENY because the `raise` happens *after* logging; (b) **single audit authority** (`audit_path=False`) so the hook is the sole logger and the report's ALLOW/BLOCK counts are exact (no double-logging). | Delivers M1: the product is not a spec but a proven closed loop — install-time membrane → runtime enforcement of the shipped constitution → project-local audit → human-readable session report. The `report` (I-20) is now meaningful on real hook output because every blocked effect carries its capability KIND. Verified 19/19 on Windows and real Linux (I-21 host). |
| I-21 | **M0 cross-OS verification on real Linux.** `run_proofs.py` was run on a real Linux host (Debian, kernel 6.1, Python 3.11) and passes **17/17**, matching Windows. The run surfaced a real defect: the agent-membrane test hard-coded a Windows-absolute path (`C:/...`) for its "write outside the project" case, which POSIX treats as a *relative* name that resolves *inside* the project — so the boundary case false-passed as ALLOW on Linux. Fixed by choosing an OS-appropriate absolute-outside path (`/etc/...` on POSIX). The engine's containment was correct on both OSes (the sibling-path case always blocked); only the test's fixture was non-portable. | Closes M0's cross-OS acceptance for Linux with an actual run, not an assertion — and demonstrates the proof suite doing its job: a real portability bug was found and fixed because the proofs were executed on another OS. macOS remains unverified (no host available). |

## Evidence

| Proves | Command | Result |
|---|---|---|
| WebAssembly hard membrane (all capability kinds) | `python products/app_membrane/run_wasm_demo.py` | 4 ALLOW / 5 DENY across FS write+read / NET / ENV / SUBPROCESS, scope-enforced + physical check, exit 0 |
| Self-falsification (anti-slop) | `python evidence/run_falsification.py` | mutation flips the decision; sabotaging guard.check() fails a proof; wasmtime refuses an ungranted syscall, exit 0 |
| Property-based fuzz | `python evidence/run_fuzz.py` | 5000 random cases: 0 deny-by-default violations, 0 disagreements with an independent oracle, exit 0 |
| Unbypassability | `python products/app_membrane/bypass_proof.py` | `unknown import` link error, exit 0 |
| Real program under WASI | `python products/app_membrane/wasi/run_wasi_demo.py` | read/write granularity: READ + WROTE + 2 DENY (write to read-only input refused), exit 0 |
| Real app: real work + containment | `python products/app_membrane/run_real_app_demo.py` | a real Rust log analyzer computes the correct stats for known input; out-of-grant read refused, exit 0 |
| Code -> constitution -> enforcement | `python evidence/demos/run_synth_demo.py` | synthesized policy bounds the app; an undeclared effect (subprocess) is denied by default, exit 0 |
| Dogfood: synthesize from own code | `python evidence/demos/run_dogfood_demo.py` | real constitution from ~36 of this repo's files, matching its actual effects, exit 0 |
| CLI end-to-end product flow | `python evidence/run_cli_e2e.py` | the real CLI: synthesize -> gate (refused) -> ratify -> gate (allowed) -> report, exit 0 |
| Warden product loop (M1) | `python evidence/run_product_e2e.py` | the REAL hook enforces the shipped constitution over a realistic session (6 ALLOW / 5 BLOCK), writes a project-local `.metaspace/` audit, and `metaspace report` summarizes it by capability KIND (FS/SHELL/NETWORK); exit 0 |
| MCP adapter parity (M2) | `python evidence/run_mcp_e2e.py` | the Claude Code hook and the generic MCP broker, driven over their real interfaces, reach identical verdicts on every effect (one shared core); the broker refuses AND does not perform a denied fs_write; exit 0 |
| SandboxEnforcer / Landlock (M3) | `python evidence/run_landlock_demo.py` | a stock native program (python) is Landlock-confined to its `.bio` write scope: it writes the granted dir, is DENIED writing elsewhere (kernel EACCES), the denied file is never created; runs on Linux, skips elsewhere; exit 0 |
| Team / CI gate (M4) | `python evidence/run_team_gate_e2e.py` | the real `metaspace gate` as CI: unratified `.bio` → build red; ratified → green; broadened-after-ratify → TAMPERED → red; exit 0 |
| Content-bound ratification | `python evidence/demos/run_ratify_demo.py` | SYNTHESIZED -> RATIFIED -> TAMPERED when the policy is widened after ratifying, exit 0 |
| Ratification gate (production) | `python evidence/demos/run_gate_demo.py` | synthesized/tampered REFUSED, ratified RUNS and enforces, exit 0 |
| Epistemic hard tier | `python evidence/demos/run_knowledge_demo.py` | 2 ALLOW / 5 DENY, exit 0 |
| Epistemic soft tier | `python evidence/demos/run_entailment_demo.py` | flags SUPPORTED / UNSUPPORTED, never blocks, exit 0 |
| Agent membrane | `python products/ai_membrane/test_hook.py` | 17/17 (incl. obfuscated commands caught structurally), exit 0 |
| Structural shell policy | `python products/ai_membrane/test_shell_policy.py` | resolves real program names; catches obfuscations the substring denylist misses; blocks interpreter-wrapper passthrough; fails closed, exit 0 |
| Friendly Fire RCE unreachable (AI Now brief) | `python evidence/run_friendly_fire_proof.py` | the REAL Warden hook over the full attack on a victim repo: 7/7 code-execution paths BLOCKED (direct script/binary + every shell-wrapper), exfil + out-of-project persistence BLOCKED, legit work still ALLOWED — the RCE effect never occurs, exit 0 |
| Dry-run learning mode | `python evidence/demos/run_dryrun_demo.py` | static-only constitution false-positive-DENIES a real write; dry-run-augmented ALLOWS it, exit 0 |
| Threat-model matrix | `python evidence/demos/run_threat_matrix_demo.py` | each layer's real check per vector; hard layers PASS a fabricated fact, soft only FLAGS it, exit 0 |
| Ratification cognitive brake | `python evidence/demos/run_ratification_review_demo.py` | a smuggled, unjustified dry-run capability cannot be ratified; a justified one can, exit 0 |

The evidence is a reproducible run, not a document: `python run_proofs.py` reproduces all of the above.
